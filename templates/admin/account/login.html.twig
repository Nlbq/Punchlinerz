<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion à l'administration</title>
    <link rel="stylesheet" href="https://bootswatch.com/5/darkly/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">

    <style>
        body{
            display: flex;
            height: 100vh;
        }
        .card{
            margin-top: 20%;
        }
    </style>
</head>
<body>
    <div class="container col-md-6 mx-auto ">
        <div class="card bg-dark p-5">
            <div class="card-header text-center">
                <h4 class="card-title">
                <i class="bi bi-lock"></i>
                    Connexion à l'administration
                </h4>
            </div>
            <div class="card-body">
                {% if hasError %}
                    <div class="alert alert-danger text-center">
                        <h5>Les informations fournies sont incorrectes</h5>
                    </div>
                {% endif %}
                <form method="POST">
                    <input type="hidden" name="go_to" value="{{ path('admin_punchs_index') }}" />
                    <div class="form-group"><label for="_username" class="py-2">Nom d'utilisateur</label><input type="text" class="form-control mb-3" placeholder="Nom d'utilisateur..." name="_username"></div>
                    <div class="form-group"><label for="_password" class="py-2">Mot de passe</label><input type="password" class="form-control mb-3" placeholder="Mot de passe..." name="_password"></div>
                    <div class="form-group text-center">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-key me-2"></i>
                            Connexion
                        </button>
                        <a href="{{ path('homepage') }}" class="btn btn-link">
                            <i class="bi bi-house-door"></i>
                            Retour au site
                        </a>
                    </div>
                </form>   
            </div>
        </div>
    </div>
</body>
</html>